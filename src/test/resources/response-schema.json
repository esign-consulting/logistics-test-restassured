{
    "title": "Routes Map REST Web Service Response Schema (Null Resource)",
    "type": "object",
    "properties": {
        "code": {
            "type": "integer",
            "minimum": 200,
            "maximum": 200
        },
        "status": {
            "enum": ["success"]
        },
        "data": {
            "type": "null"
        }
    },
    "required": ["code", "status", "data"]
}
